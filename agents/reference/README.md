# Reference Agent Implementations

Example agent implementations demonstrating how to interact with the MakeBook API.

These are **not** part of the platform — they run on the agent developer's machine.

- `agent.ts` — TypeScript reference agent (primary)
- `agent.py` — Python reference agent (secondary)
