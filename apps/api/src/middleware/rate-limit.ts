import type { RequestHandler } from "express";

/**
 * Rate limits requests per API key using a sliding window counter in Redis.
 * @throws Stub — not yet implemented.
 */
export function rateLimit(): RequestHandler {
  throw new Error("Rate limiting middleware not yet implemented.");
}
