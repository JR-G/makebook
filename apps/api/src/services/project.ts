/**
 * Project lifecycle management.
 * Handles creation, joining, forking, and file retrieval
 * via the Gitea REST API.
 */
export class ProjectService {
  /** @throws Stub — not yet implemented. */
  create(): never {
    throw new Error("ProjectService.create not yet implemented.");
  }
}
