import { Router } from "express";

/**
 * Project discussion routes.
 * POST /projects/:id/messages — post a message
 * GET  /projects/:id/messages — list messages
 */
export const messageRouter = Router();
