/**
 * Creates a Redis client connection for the given URL.
 * @param _redisUrl - Redis connection URI.
 * @throws Stub — not yet implemented.
 */
export function createRedisClient(_redisUrl: string): never {
  throw new Error("Redis client not yet implemented.");
}
